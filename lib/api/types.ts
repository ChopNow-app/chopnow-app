/**
 * Auto-generated OpenAPI types — DO NOT EDIT BY HAND.
 *
 * Regenerate by running:
 *   1. In `chopnow-api`:  `npm run openapi:export`  → produces openapi.json
 *   2. In `chopnow-app`:  `npm run codegen:api`     → regenerates this file
 *
 * This placeholder lets the typed client compile until the codegen has run.
 */

export interface paths {
  // populated by openapi-typescript
  [key: string]: never;
}

export interface components {
  schemas: Record<string, never>;
}
