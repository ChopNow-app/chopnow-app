import { VendorDashboard } from '@/features/vendor/components/VendorDashboard';

// Stories 2.4 / 2.7 / 2.10 / 3.7 — vendor operational dashboard.
export default function VendorPage() {
  return <VendorDashboard />;
}
