import { AddressesPage } from '@/features/consumer/components/AddressesPage';

// Story 3.2 — saved-address CRUD UI. Auth-gated at the component level.
export default function AddressesRoute() {
  return <AddressesPage />;
}
