// Sprint 1 — Story 2.5 / 2.6 (Catalogue browse, vendor list).
// Real implementation lands in `features/consumer/components/` and is imported here.

export default function RestaurantsPage() {
  return (
    <main className="min-h-dvh bg-chop-warm p-4 text-chop-ink">
      <h1 className="text-2xl font-bold">Restaurants</h1>
      <p className="text-sm text-muted-foreground">
        Liste des vendeurs autour de toi — en construction (Story 2.5).
      </p>
    </main>
  );
}
